os:
    ubuntu

language: python
python:
    - 2.7
    - 3.6

before_install:
    - sudo apt-get update 

install:
    - sudo apt-get -y install pocl
    - pip install pyopencl
    - python -c 'import pyopencl as cl'
    - pip install beaker tox tox-travis

script:
    - tox
